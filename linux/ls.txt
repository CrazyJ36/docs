#ls command usage.

ls	shows all file and folder names
ls -a	shows hidden files and indicates a directory clearly
ls -l --author   shows last time modified, permissions, and author
ls -l --author   like above, but shows the creator of file as well
